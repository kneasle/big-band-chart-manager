[project]
name = "part-splitting"
version = "0.0.0"
description = "Splitting single PDFs of Big Band charts into per-part PDFs."
requires-python = ">=3.12"
dependencies = [
    "opencv-python>=4.13.0.92",
    "pdf2image>=1.17.0",
    "pypdf>=6.7.0",
]

[tool.ruff]
line-length = 100
